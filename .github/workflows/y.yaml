name: Y

on:
  push:
    branches:
      - main
    paths:
      - y/*.txt

jobs:
  call_ci:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Call reusing CI
        uses: ./.github/workflows/ci.yml
        with:
          file: y.txt
